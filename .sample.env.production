# @TODO If you are trying to deploy this application, be sure that this file has
# been copied to `.env.production` and the appropriate values have been filed
# in.
#
# Secret environment variables in use in production.
# During the initial deploy to Heroku these variables will be copied onto the
# environment for the application running on Heroku.

SECRET_KEY=
ENVIRONMENT=PRODUCTION
